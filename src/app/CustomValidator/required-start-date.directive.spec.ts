import { StartEndDateComparisonDirective } from './required-start-date.directive';

describe('RequiredStartDateDirective', () => {
  it('should create an instance', () => {
    const directive = new StartEndDateComparisonDirective();
    expect(directive).toBeTruthy();
  });
});
